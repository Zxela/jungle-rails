<tr>
  <td>
    <h4 class="admin-cat"><%= category.name %></h4>
   <div class="delete-me"><%= link_to fa_icon('trash'), [:admin, category], class: 'btn btn-sm btn-danger', method: :delete, data: { confirm: "Are you sure?" } %></div>
  </td>
</tr>